<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
  <% end %>
  <% @students.each do |student| %>
    <div><%= link_to student, students_path(@student) %></div>
  <% end %>

  <% @students.each do |student| %>
    <div><%= link_to student, "/students/#{student.id}" %></div>
  <% end %>
</div>